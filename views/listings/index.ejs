<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listing page</title>
    <style>
        div{
            height: 250px;
            width: 250px;
            background-color: antiquewhite;
            border: 2px solid black;
            border-radius: 8px;
            margin: 15px;
            padding: 5px;
        }
        .container{
            
            height: auto;
            width: auto;
            background-color: aliceblue;
            border: 0px;
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            padding: 0px;
            margin: 0px;
        }
        img{
            max-width: 100%;
            max-height: 150px;
            object-fit: cover;
            border-radius: 4px;
            margin-top: 8px;
        }
    </style>
</head>
<body>
    <h1>All Listings</h1>
    <form method="get" action="/listing/new">
        <button>Create New Listing</button>
    </form>
    <div class="container">
        
            <% for(let i=0;i< allListing.length; i++){ %>
                <div>
                    <a href="/listing/<%=allListing[i]._id %>"><%= allListing[i].title %></a>
                    <img src="<%= allListing[i].image %>" alt="Listing image">
                    <form method="post" action="/listing/<%=allListing[i]._id %>/delete" _method="delete">
                        <button>Delete</button>
                    </form>
                </div>   
            <% } %>
        
    </div>
    
    
    
</body>
</html>